java - 修饰符类型

修饰符是关键字，因此你可以添加这些定义去改变他们的含义。Java语言有各种各样的修饰符，包括以下 ——

    java 访问修饰符

    无访问修饰符

使用修饰符，必须在类，方法或者变量定义时包含关键字。

访问控制修饰符

Java 提供一些访问修饰符设置类，变量，方法和构造函数的访问级别。这四个访问级别是 ——

    默认可见到包，。它不需要修饰符。
    
    仅仅可见到类（private）

    可见到一切（public）

    可以到包和子类（protected）

非访问修饰符

Java 提供一些非访问修饰符完成许多其它的功能。

	创建类方法和变量的静态修饰符。

	最终修饰符用于


