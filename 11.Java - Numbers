Java - Numbers Class

通常的，当我们使用Numbers时，我们使用基本的数据类型，比如自己字节类型、整型、长整型，浮点型等等。

然而，在开发中，我们遇到的情况是使用对象类型而不是基本数据类型。
为了实现这一目标，Java提供包装类。

所有的包装类（Integer、Long、Byte、Double、Float、Short)都是抽象类型Number的子类。

包装类对象包含或者包装它们各自的基本数据类型。
